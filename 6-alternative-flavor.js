// module.exports.items = ["item1", "item2"];

// const names = require("./2-global");
const names = require("./2-global");

if (names < names.length(1)) {
  console(names);
} else {
  console.log(John);
}
